# Git Metrics

## Running Locally

1. Run `./run.sh -o git.sqlite -r ../workflows-conductor`  to make the csv file
2. Run queries against the `metrics` table

Use `sqlite3 git.sqlite` to open an existing database.
